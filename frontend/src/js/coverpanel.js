/* ==========================================================================
   coverpanel.js — 兼容入口（实现已迁到 ui/cover.js）
   --------------------------------------------------------------------------
   调用方（曲目「更多」菜单、播放详情页头部按钮）历史上从 `./coverpanel.js`
   动态 import openCoverPanel，保留这个入口可以让改动面集中在 UI 层。
   ========================================================================== */

export { openCoverPanel, closeCoverPanel } from "./ui/cover.js";
