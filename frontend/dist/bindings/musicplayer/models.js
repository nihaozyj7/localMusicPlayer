// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * CoverItem 一张封面（缓存里的可编辑项，或文件内嵌的只读项）。
 * @typedef {Object} CoverItem
 * @property {number} index - 缓存项=0..n-1；内嵌项=-1-i（负数，只读）
 * @property {string} preview - data URL
 * @property {string} source
 * @property {string} [provider]
 * @property {string} [mime]
 * @property {number} [width]
 * @property {number} [height]
 * @property {number} [bytes]
 * @property {number} [at]
 * @property {boolean} [embedded]
 * @property {boolean} active
 */

/**
 * Result 一次封面操作的结果（前端弹层直接用这个渲染）。
 * @typedef {Object} CoverResult
 * @property {boolean} ok
 * @property {string} provider
 * @property {number} score
 * @property {string} source
 * @property {string} preview - data URL，直接喂给 <img>
 * @property {string} message
 * @property {boolean} embedded - 是否已写回歌曲文件
 * @property {number} [width] - Width/Height 图片实际尺寸（校验时解出来的，界面可以显示「500×500」）
 * @property {number} [height]
 */

/**
 * CoverSet 一首歌的封面集合。
 * 
 * 这里没有「轮播开关」：轮播是全局偏好（bootstrap.Config.CoverCarousel），
 * 前端根据自己的设置决定要不要轮换显示 Items。服务端只负责给数据。
 * @typedef {Object} CoverSet
 * @property {string} id
 * @property {CoverItem[] | null} items - 缓存里的（可增删/切换）
 * @property {CoverItem[] | null} embedded - 文件内嵌的（只读展示）
 * @property {number} active
 * @property {string} [message]
 */

// In interface mode, this file is likely to contain just comments.
// We add a dummy export statement to ensure it is recognised as an ES module.
export {};
