// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * DownloadService 把在线歌曲保存到本地。
 * 
 * 为什么下载要放到后端而不是前端 <a download>：
 *  1. 下载目录要可配置（默认是系统「音乐」目录下的 downloads），只有 Go 侧知道真实路径；
 *  2. 前端 <a download> 受同源/重定向限制，且无法反馈「存到哪了」「下了多少」；
 *  3. 后端可以直接复用音频流解析（bilibili.ResolveAudio），写入目标目录时还能
 *     做重名处理与「临时文件 + 原子改名」（中途失败不会留下半个文件）。
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "/wails/runtime.js";

/**
 * ApplyDir 真正应用新的下载目录；migrate=true 时把已有文件搬过去。
 * 
 * 返回 {dir, migrated, failed, skipped}：
 *   - migrated 成功搬走的文件数；
 *   - skipped  目标目录已存在同名文件（不覆盖，保留原文件）；
 *   - failed   迁移失败的文件名。
 * 
 * 无论迁移结果如何，目录本身一定会切换成功 —— 迁移失败不该阻止用户改设置。
 * @param {string} dir
 * @param {boolean} migrate
 * @returns {$CancellablePromise<{ [_ in string]?: any } | null>}
 */
export function ApplyDir(dir, migrate) {
    return $Call.ByID(1592245466, dir, migrate);
}

/**
 * Dir 返回当前下载目录（并确保它存在）。
 * @returns {$CancellablePromise<string>}
 */
export function Dir() {
    return $Call.ByID(1582288212);
}

/**
 * OpenDir 在系统文件管理器里打开下载目录。
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function OpenDir(path) {
    return $Call.ByID(3020728472, path);
}

/**
 * PickDir 弹出系统目录选择器，**先不写入配置**，把决定权留给用户。
 * 
 * 为什么不在选完目录就直接改：用户要求「更改下载路径时提示是否把已下载的歌曲
 * 迁移过去」。所以这里只返回候选目录与现状统计，由前端弹确认框，用户选完再调
 * ApplyDir（含是否迁移）。
 * 用户取消选择时返回 cancelled=true。
 * @returns {$CancellablePromise<{ [_ in string]?: any } | null>}
 */
export function PickDir() {
    return $Call.ByID(3118758287);
}

/**
 * SetDir 直接指定下载目录（手动输入路径 / 恢复默认）。同样只返回提案，不落盘。
 * @param {string} dir
 * @returns {$CancellablePromise<{ [_ in string]?: any } | null>}
 */
export function SetDir(dir) {
    return $Call.ByID(63808066, dir);
}

/**
 * Start 开始下载一首在线歌曲（异步）。
 * 
 * 立即返回 started=true；进度与结果通过 download:progress / download:done /
 * download:failed 事件推送，与扫描的交互方式保持一致。
 * @param {string} bvid
 * @param {string} title
 * @param {number} durationMS
 * @returns {$CancellablePromise<{ [_ in string]?: any } | null>}
 */
export function Start(bvid, title, durationMS) {
    return $Call.ByID(1672047263, bvid, title, durationMS);
}

/**
 * Status 返回下载目录与正在下载的任务（供设置界面展示）。
 * @returns {$CancellablePromise<{ [_ in string]?: any } | null>}
 */
export function Status() {
    return $Call.ByID(2563507631);
}
