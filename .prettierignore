node_modules/
frontend/dist/
frontend/bindings/
frontend/.bindings-tmp-*/
bin/
build/ffmpeg/
internal/ffmpeg/bin/
internal/theme/builtin/
.task/
*.exe
*.exe~

# 生成物：格式化只会制造噪音
docs/screenshots/

# Go 侧不做 Prettier（由 gofmt 负责）
*.go
go.mod
go.sum

# --------------------------------------------------------------------------
# 尚未纳入格式化的区域（历史遗留，属"逐步收紧"的一部分）
# --------------------------------------------------------------------------
# 只有下面两处的理由成立，因此保留在忽略列表里：
#   · styles/ 里大量数值是量出来的（见 tools/measure-*.mjs 的量测报告），
#     Prettier 会重排声明、拆并多值属性，虽然语义等价但会让量测对比失去意义；
#   · src/*.html（index / lyrics / wallpaper）里内联元素的空格是**有语义的**
#     （htmlWhitespaceSensitivity），自动格式化会改变行内间距，属于会真的改画面的改动。
#     三张页面用的是同一套写法，所以整个 *.html 一起排除，而不是只排 index.html。
#
# ★ 这里原本写的是整目录的 "frontend/src/" —— 那个模式把 frontend/src/js/
#   （全部业务逻辑）也一起排除了，而它既没有「量测出来的数值」，也没有
#   「有语义的空格」。范围大于理由的直接后果：settings.js 用「无分号」风格，
#   而其他所有模块用分号，且永远不会被自动修正。现在按理由精确收窄。
frontend/src/styles/
frontend/src/*.html
