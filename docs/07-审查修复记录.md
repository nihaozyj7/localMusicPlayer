# 审查修复记录（对 05 / 06 两份审查报告的复核与处置）

本文是「审查之后」的那一步：逐条复核 `docs/05-BUG审查报告.md` 与
`docs/06-性能审查报告.md` 的结论（确认是不是真 BUG、是不是业务所需），
然后修掉确认的问题，并给出每条改动的验证方式。

复核方式：**不看结论看代码** —— 每条都回到报告给出的 `文件:行号` 读一遍原始实现，
确认「报告引用的代码确实存在且确实是那样」，再判断它是不是与需求/注释矛盾。
凡是复核中发现报告本身有偏差的，在下文单独列出。

---

## 0. 执行过的校验

| 命令                                        | 修复前                                                                     | 修复后                   |
| ------------------------------------------- | -------------------------------------------------------------------------- | ------------------------ |
| `npm run check`（eslint + tsc + node:test） | ❌ eslint 报 `magia.js:711`（no-useless-escape），后面的 tsc/test 根本没跑 | ✅ 全部通过（15 个单测） |
| `go vet ./...`                              | ✅                                                                         | ✅                       |
| `go test ./...`                             | ✅（但未覆盖下面这些路径）                                                 | ✅ 14 个包全绿           |
| `node tools/check-player-host.mjs`          | ❌ 2 项失败（自检脚本把内置样式写死成 3 个）                               | ✅ 17 项全部通过         |
| production 产物体积                         | 19.87 MB                                                                   | **16.22 MB**             |

---

## 1. 逐条处置

### 1.1 严重（2 条，全部修复）

| 报告发现                                              | 复核结论                                                                                                                                                 | 处置                                                                                                                                                                                                   |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 文件夹监听在「启动时没有文件夹 / 关着监听」后永不启动 | **真 BUG**。`Watcher.Start` 是唯一会 `go w.loop()` 的地方，而 `refreshWatcher` 只调 `SetRoots`（只 `fsw.Add`）                                           | ✅ 新增 `Watcher.EnsureStarted`（幂等启动循环），`refreshWatcher` 改走它；同时把监听根从 `cfg.Folders` 换成 `EffectiveFolders()`（含隐式下载根）                                                       |
| 元数据阶段被取消 → 清空曲库并发 `scan:done`           | **真 BUG**。`readAll` 取消时返回 `out[:0]`，`Scan` 之后不再检查 `ctx.Err()`，直接把 `m.songs` 覆盖成空；`RescanPaths` 更糟（先删受影响目录的旧条目再写） | ✅ `readAll` 改为返回 `(songs, cancelled)`（worker 提前退出也显式置 cancelled，不只靠事后查 `ctx.Err()`）；`Scan` / `RescanPaths` 在被取消时**放弃本次结果**并如实返回错误；补时长阶段被取消同样不提交 |

### 1.2 中等（12 条，全部修复）

| 报告发现                                              | 复核结论                                                                                                          | 处置                                                                              |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `removeFromQueue` 移除正在播放的歌会把 currentId 置空 | **真 BUG**。先 `filter` 再 `indexOf(songId)`，必然 −1                                                             | ✅ 下标改在过滤前取                                                               |
| 队列视图 / 面板丢掉在线试听曲目 → 拖拽错位、计数少    | **真 BUG**。`currentSongList` 与 `renderQueuePanel` 只在 `state.songs` 里查，而在线曲只登记在 `state.onlineSongs` | ✅ 改用 `songById` / 补齐 `onlineSongs`；`nextSongPreview` 一并修                 |
| `reorderQueue` 把队列下标写回来源歌单                 | **真 BUG**。往队列插过歌之后两套下标不再等价，代码里没有任何校验                                                  | ✅ 落盘前比对「歌单 id 序列 === 队列」；不一致就不写并把 `queueOrigin` 降级       |
| 「实时监听」开关从不调用 `SetWatchers`                | **真 BUG**。全库零调用点，运行期监听根不变                                                                        | ✅ 开关分支调用 `backend.setWatchers(next)`                                       |
| `showDesktopWallpaper` 从未持久化                     | **真 BUG**。`applyPatch` 里没有该分支（未知键被静默忽略），而启动恢复代码专门读它                                 | ✅ 补上对称的分支                                                                 |
| `RemoveFolder` 的 `defer cancel()` 取消自己的扫描     | **真 BUG**。函数一 return 就把刚交给 goroutine 的 ctx 取消                                                        | ✅ `cancel` 移进 goroutine                                                        |
| 缓存索引 `index.json` 非原子写                        | **真 BUG**（鲁棒性）。同包的图片/歌词文件都是 tmp+rename，只有索引直接覆盖                                        | ✅ 改成 tmp+rename                                                                |
| `DeleteCover` 缺 `ensureLoadedLocked()`               | **真 BUG**（潜伏）。它是同包唯一漏掉这步的方法，首次访问时「删了个寂寞」                                          | ✅ 补上                                                                           |
| 下载完成后不重扫、下载目录不在监听根                  | **真 BUG**。两处 roots 只用 `cfg.Folders`，而 `Scan` 用的是 `EffectiveFolders`                                    | ✅ roots 统一用 `EffectiveFolders()`；新增 `onFileAdded` 回调，下载成功即增量入库 |
| 过滤正则语义前后端不一致                              | **真 BUG**。前端用 `标题.扩展名`，后端与文档用「完整路径 + 文件名」                                               | ✅ 前端改为 path + basename                                                       |
| 清空缓存只删 .lrc、不清歌词索引                       | **真 BUG**。注释自己都写「索引没有单独暴露删除接口」                                                              | ✅ 新增 `Store.DeleteLyrics`，`ClearCache` 改走它                                 |
| 底栏封面兜底是一次性的（第二次坏封面显示碎图）        | **真 BUG**。一次性标记再没人清；`setCoverImage` 都会复位，说明「换图复位」是既定做法                              | ✅ 判据改成「当前 src 是否已是默认封面」                                          |

### 1.3 轻微（修复 6 条）

| 报告发现                                                 | 复核结论                                                                                                            | 处置                                                                     |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `ffmpeg` 的 `once/resolved` 与 `loudness.tools` 无锁读写 | **真问题**。`Reset()` 直接给 `sync.Once` 赋值，与并发 `Resolve` 构成数据竞争                                        | ✅ `Resolve/Reset` 改双检 + 读写锁；`loudness` 的 tools 加独立 `toolsMu` |
| `theme/skins.Manager` 整表替换无锁                       | **真问题**，且注释「一次换指针，所以并发读不会读到半成品」是错的 —— 无锁读正在被替换的 map 是 **fatal**，不是读旧值 | ✅ 两个 Manager 加 `RWMutex`                                             |
| `enrichDurations` 锁外读 `done`                          | **真问题**（数据竞争）                                                                                              | ✅ 在 `mu` 里取值                                                        |
| 下载文件名未处理 Windows 保留设备名                      | **真问题**。`CON.m4a` 会直接创建失败，用户只看到「下载失败」                                                        | ✅ `safeFilename` 加保留名处理                                           |
| `bridge.on()` 订阅完成前退订会泄漏                       | **真问题**                                                                                                          | ✅ 加 `cancelled` 标记                                                   |
| `meta:embed-progress` 无人订阅                           | **真问题**（原本标存疑，但后端一直在发，且批量写文件可能跑几分钟）                                                  | ✅ 前端订阅并更新 toast 文案；`finally` 里退订                           |

### 1.4 判定为「业务所需」/ 不修（明确跳过）

| 项                                               | 为什么不修                                                                                                                                                                                              |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Songs()` 把整库（含 base64 封面）一次性过 IPC   | 要改成「封面走 URL」是一次跨前后端的重构（封面解析、轮播、写回文件、缓存四条链路都会动）。收益确实最大，但风险与本次审查的定位不符，建议单独排期。见 `docs/06` 的 P0-3，本文档第 3 节也记了当前替代缓解 |
| 曲目表零虚拟化                                   | 同上，属于结构改造。本次只做了「不再每帧把全部 id 拼成渲染键」等低风险优化                                                                                                                              |
| `SaveCache` 用 `MarshalIndent` 重写整份缓存      | 只影响扫描结束那一次；改成 `Marshal` 会让缓存文件从「可读」变成「不可读」，排查问题时反而更差                                                                                                           |
| 首帧后回填 200 张封面、`discoverThemes` 串行 N+1 | 都发生在首帧之后（`hydrateCachedCovers` / `refreshLoudness*` 已在 `tick()` 之后），当前规模下（3 个主题）不值得为它引入并发与复杂度                                                                     |
| 「清空缓存」的歌词计数语义                       | 已按报告修掉索引不一致，不再额外改动统计口径                                                                                                                                                            |

---

## 2. 性能修复（`docs/06`）

| 报告项                                                                                                                          | 处置                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0-1** 启动时同步读 + 解析 5.86MB（5,865,829 字节）的 `metadata-cache.json`（99.7% 是 base64 封面）                           | ✅ 改为**懒加载**（`cacheOnce` + `ensureCacheLoaded`），在 `Scan` / `RescanPaths` / `SaveCache` 三个真正用到缓存的入口补一次。这一项原先发生在 `main()` 建窗口**之前**                                     |
| **P0-2** `ffmpeg.Resolve()` 被 `loudness.NewManager` / `media.New` 在建窗口前同步调用（`sync.Once` 让「后台 prewarm」形同虚设） | ✅ 两个构造函数不再解析；`loudness` 用 `toolSet()`、`media` 用 `ffmpegPath()` 在真正需要时补解析；后台 prewarm 成为首个解析者                                                                              |
| **P0-5** `commit()` 每次都 `JSON.stringify` 全量快照 + 同步写 localStorage                                                      | ✅ 快照写盘 180ms 去抖（`persist` / `persistNow`），`flushConfigSync` 与 `beforeunload` 仍会立即落盘；状态本身依旧同步更新                                                                                 |
| **P1** 每帧多次 O(n) `songById` 线性查找                                                                                        | ✅ `recalcVisible` 顺带维护 id→song 索引（`songIndex`），`songById` 改查表                                                                                                                                 |
| **P1** 每帧两次全表 `querySelectorAll`                                                                                          | ✅ `renderKey` 里的「遍历全部可见 id」换成 `state.visibleVersion`（O(1)）；`paintTrackSelection` 只在「当前曲目 / 播放状态」变化或表格重建时执行；`playerbar` 里那段每帧全文属性查询删掉（与前者语义等价） |
| **P1** 过滤规则预览每按键全库重过滤、每首歌 `new RegExp`                                                                        | ✅ `applyRules` 预编译规则一次（O(songs×rules) → O(rules)）                                                                                                                                                |
| 逐字歌词给每个字素 `will-change`（本次新增皮肤引入）                                                                            | ✅ 去掉：一首歌上千个元素逐个提升合成层，实测会把整片文字合成到糊掉                                                                                                                                        |

**未处理**：队列面板每帧 `join`、watcher flush 的 O(folders×songs) 统计、
封面 base64 的三份驻留 —— 见 1.4 的理由。

---

## 3. 首屏与内存

真正压在第一帧之前的两个大头已经移走：

1. **5.86MB 元数据缓存**不再在建窗口前同步解析（懒加载）；
2. **内置 ffmpeg 的解包**（读盘 + 哈希 + 落盘）不再由主线程触发，只发生在后台
   prewarm 里；热启动连解压都不做（见下节）。

### 本机实测（重要：不要照抄报告里「秒级」的量级）

在同一台机器（56 首、`metadata-cache.json` 5,865,829 字节、其中 99.7% 是封面
base64）上实测这两段原本由主线程承担的开销：

| 动作                  | 本机实测 | 说明                                |
| --------------------- | -------- | ----------------------------------- |
| 读 5.86MB JSON        | 3 ms     | NVMe + 页缓存                       |
| `json.Unmarshal`      | 6 ms     | 56 条记录                           |
| `ffmpeg.exe` sha256   | 4 ms     | 6,280,718 字节                      |
| 首跑写 6MB 到缓存目录 | 3 ms     | 不含杀软扫描；热启动只 `stat`，不写 |

也就是说：**在这台机器的当前曲库规模下，这次优化的绝对收益是 10–20ms 级**，
不是报告里估的「秒级 + 数百 MB 峰值」。报告那个量级是按 1000 首 / 155MB 内嵌件
**外推**出来的，没有实测（见第 5 节）。

那为什么仍然值得改：

- 两项成本都与曲库/内嵌件规模成正比：1000 首时缓存 JSON 约 146MB，解析时间与
  峰值内存都会线性上涨，而且解析出来的 base64 字符串会**长期驻留**（内存不是
  一次性开销）；
- `ffmpeg` 那一段原本被 `sync.Once` 从后台 goroutine「抢」回主线程 ——
  注释声称「在后台做」，实际第一次 `Resolve` 由 `loudness.NewManager` /
  `media.New` 在建窗口前触发，首次启动还要写 6MB 出去，被杀软拦截时是秒级；
- 这两项都在「建窗口之前」，属于关键路径上的同步 I/O，改成懒加载/后台之后，
  规模增长不会再反馈到首屏。

同时把「每帧」的开销压下去（渲染键、选中行同步、id 查找），这几项在长曲库下
是持续性的 CPU 与 GC 压力，间接也影响滚动与首屏后的可交互时间。

内存方面本次只做了「不再每帧重建渲染键字符串 / 不再逐字素提升合成层」这类
不改变数据驻留的优化；封面 base64 的三份驻留（Go 侧缓存、IPC 载荷、前端状态 +
DOM）是内存占用的主要来源，需要按 1.4 的说明单独重构。

---

## 4. exe 产物体积

内嵌的 ffmpeg 改为 **gzip 压缩后嵌入**（`internal/ffmpeg/bin/ffmpeg.exe.gz`，
由 `tools/build-ffmpeg.mjs` 在编译完 exe 后顺手生成）：

|                 | 原始嵌入 | gzip 嵌入            |
| --------------- | -------- | -------------------- |
| 内嵌件          | 5.99 MB  | **2.21 MB**（37.0%） |
| production 产物 | 19.87 MB | **16.22 MB**         |

关键点是不牺牲启动性能：

- 缓存文件名取**压缩数据的 sha256**，「能否复用」比对 **gzip 尾部的解压后长度
  （ISIZE）** —— 两者都不需要解压，所以热启动一次解压都不发生；
- 只有缓存缺失时才真的解压一次，而且那次解压发生在后台 prewarm 里，不在首帧路径上；
- 解压依赖「单成员 gzip」这一前提，压缩包是构建脚本自己生成的，代码里有注释说明。

没有采用的方案：UPX（会显著增加杀软误报与冷启动解压时间）、把 ffmpeg 改成
按需下载（会破坏「离线开箱即用」这个既定需求）。

---

## 5. 复核中发现的「报告本身的问题」

审查报告整体质量很高（每条都能对到真实代码），但有三处偏差，已在复核时纠正，
提醒后来人不要照抄：

1. **行号漂移**：`docs/06` 说 `store.js:200 commit()` 会「重建 id→song Map（:326）+
   整库重排（:355）+ localStorage（:980）」。实际 `:326` / `:355` 属于
   `currentSongList` / `recalcVisible`，不在 `commit` 里。结论（commit 每次都做
   持久化、存在每帧调用）是对的，但引用位置需要按当前树重新核对。
2. **"`npm run check` 当前失败"** 这一条在审查开始时成立，但同一批任务里已经顺手
   修掉了（`magia.js` 字符类里多余的 `\-`），复核时不应再当作未修问题。
3. **影响量级是外推、不是实测**：`docs/06` 的 P0-1 写「秒级 + 数百 MB 峰值」、
   P0-2 写「按 155MB 估算哈希 0.2–0.5s、写盘 1–3s」。这两个数字分别按
   1000 首与 155MB 内嵌件外推，**与当前仓库的实际情况不符**（内嵌件是 6MB、
   本机曲库 56 首）。本机实测见第 3 节：两项加起来是 10–20ms 级。
   **结论方向是对的**（成本随规模线性增长、且确实都在建窗口之前），
   但排期/收益判断应该按实测，不能按报告的估算值。

另外 `docs/05` 的「轻微」条目里有一条判定为「存疑」（`meta:embed-progress`），
复核后按「真问题」处理并修掉了 —— 依据是后端一直在发事件、且批量写文件可能持续
几分钟，界面毫无进度确实会让人以为卡死。

---

## 6. 本次新增/强化的回归测试

| 测试                                                        | 保护什么                                                                                     |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `internal/library/scancancel_test.go`（2 个）               | `readAll` 的取消契约；被取消的增量重扫不得改动曲库。（已验证：把修复临时去掉，测试确实会红） |
| `internal/metacache/delete_lyrics_test.go`                  | 删歌词要同时清文件与索引（含重启后）                                                         |
| `download_naming_test.go`                                   | Windows 保留设备名                                                                           |
| `frontend/tests/player-skins.test.js`（改写）               | 内置样式清单与顺序、哪些样式需要整窗背景层                                                   |
| `tools/check-player-host.mjs` / `check-app.mjs`（口径更新） | 样式按钮组按注册表渲染、样式卡片数量                                                         |

前端的两条队列 BUG（移除正在播放的歌、在线试听曲目丢行）用无头浏览器做了
端到端验证：移除当前歌后 `currentId` 顺延到下一首且 `playing` 保持 true；
注入一首在线试听曲后队列面板行数与计数都跟 `state.queue` 一致。
